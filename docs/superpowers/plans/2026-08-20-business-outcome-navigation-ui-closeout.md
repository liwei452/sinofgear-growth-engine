# UI Closeout Execution Plan

1. Merge the latest `merge/consolidation-security` base and regenerate the frontend API schema from the merged OpenAPI document.
2. Replace the Results placeholder funnel with tested, persisted attribution metrics and scope disclosure.
3. Group Publishing into three primary stages while retaining seven backend statuses as secondary filters; add permission-aware empty actions.
4. Add the two-path Opportunity empty state, switch core headers to light medical blue, and flatten Today card hierarchy.
5. Update E2E coverage for three-stage mobile navigation and generate eight consistent review screenshots.
6. Run focused and full frontend tests, backend tests, typecheck, lint, API schema check, production build, and E2E.
7. Commit and push normally to the existing PR head, update the Draft PR evidence, and do not merge.
